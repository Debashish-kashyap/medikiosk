--index-url https://download.pytorch.org/whl/cpu
--extra-index-url https://pypi.org/simple
# Core (needed to run the demo). Kept minimal so `pip install` is fast and reliable.
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-multipart>=0.0.9
cryptography>=42.0
# API test client dependency used by FastAPI/Starlette TestClient.
httpx>=0.27
metaphone>=0.6

# --- Optional / production.
# redis>=5.0            # session store with TTL (Data/Security lane)
# ASR Options (Lane 4):

transformers>=4.35.0   # AI4Bharat IndicWhisper models (Lane 4)
torch>=2.0.0           # PyTorch for IndicWhisper (Lane 4)
faster-whisper>=1.0    # offline ASR fallback (Lane 4). Requires ffmpeg on PATH.
google-genai>=2.0.0      # Official Google GenAI SDK (Lane 5 - Multimodal Vision & NLU)
google-generativeai>=0.8.0 # Google AI Studio Gemini API (Legacy fallback)
httpx>=0.27.0           # HTTP client for Gemini API & Async requests
# fhir.resources>=7.1   # validate/serialize FHIR R4 (Data/Security lane)
pytesseract>=0.3.13    # fallback printed-document OCR (AI-OCR lane)
pillow>=10.0.0         # image processing for OCR (AI-OCR lane)
pymupdf>=1.24.0        # PDF rendering without system dependencies (AI-OCR lane)

